' Gambas class file

Public str_debug As String

Public Sub Form_Open()
  Me.X = Desktop.W - Me.W
  Me.Y = 0
  'live_bg.Animation = Animation.Load("new.gif")
  Shell "pirivoice.py"
End

Public Sub btn_close_MouseDown()
  FMain.Close
End

Public Sub btn_about_MouseDown()
  FAbout.Show
End


Public Sub Button1_Click()
  FAbout.Show
End



Public Sub PictureBox1_MouseDown()
  Dim x As String
  Shell "pgrep pirivoice | wc -l" To x
  
  If (Val(x) <> 0) Then
    Shell ("kill $(pgrep pirivoice)")
    
  
  Else 

  Shell ("pirivoice.py -s")
  
  Endif
End